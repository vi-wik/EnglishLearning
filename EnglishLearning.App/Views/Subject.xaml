<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EnglishLearning.App.Views.Subject"
             xmlns:controls="clr-namespace:EnglishLearning.App.Controls"
             Title="">
    <ScrollView>
        <VerticalStackLayout>

            <Label x:Name="lblDescription"  VerticalOptions="Center"  HorizontalOptions="Start" Margin="5,2"/>

            <Label x:Name="lblIntroductionTitle" Text="相关介绍"  VerticalOptions="Center" HorizontalOptions="Start" FontAttributes="Bold" Margin="5,10"  />

            <CollectionView x:Name="lvMedias" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <controls:MediaCardView></controls:MediaCardView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label x:Name="lblDetailsTitle" Text="详细内容"  VerticalOptions="Center" HorizontalOptions="Start" FontAttributes="Bold" Margin="5,10"  />

            <CollectionView x:Name="lvTopics" EmptyView="" SelectionMode="None" >
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="5,5,8,8" HorizontalOptions="Center">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Tapped="TapGestureRecognizer_TopicTapped" NumberOfTapsRequired="1" />
                            </Grid.GestureRecognizers>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"  />
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="320" />
                            </Grid.ColumnDefinitions>

                            <Border Padding="5">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="4,4,4,4" />
                                </Border.StrokeShape>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"  />
                                        <RowDefinition Height="Auto"  />
                                    </Grid.RowDefinitions>

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Label Text="{Binding Name}" Grid.Row="0" FontAttributes="Bold"></Label>
                                    <Label Text="—" Grid.Column="1" FontAttributes="Bold" Margin="20,0"></Label>
                                    <Label Text="{Binding Name_EN}" Grid.Column="2" FontAttributes="Bold" ></Label>
                                    <Label Text="{Binding Description}" Grid.ColumnSpan="3" Grid.Row="1" Margin="0,5"  FontSize="12"></Label>
                                </Grid>

                            </Border>
                        </Grid>

                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>