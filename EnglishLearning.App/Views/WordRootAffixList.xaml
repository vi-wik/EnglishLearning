<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EnglishLearning.App.Views.WordRootAffixList"          
             Title="">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"></ColumnDefinition>
                <ColumnDefinition Width="7.5*"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Picker Title="" x:Name="picker" Grid.Column="0"  HeightRequest="50" HorizontalTextAlignment="Center" BackgroundColor="AliceBlue" SelectedIndexChanged="picker_SelectedIndexChanged">
                <Picker.Items>                    
                </Picker.Items>
            </Picker>

            <Entry x:Name="txtKeyword" Grid.Column="1" Text="" Placeholder="请输入要查找的内容" PlaceholderColor="LightGray" HorizontalTextAlignment="Start" Keyboard="Default"  ClearButtonVisibility="WhileEditing" Completed="txtKeyword_Completed" HeightRequest="50"></Entry>

            <ImageButton  x:Name="btnSearch"  Grid.Column="2"  WidthRequest="20" HeightRequest="20" Clicked="OnSearchButtonClicked">
                <ImageButton.Source>
                    <FontImageSource   FontFamily="FASolid" Glyph="{StaticResource search}"  Color="DodgerBlue" Size="16"/>
                </ImageButton.Source>
            </ImageButton>
        </Grid>      

        <Grid Grid.Row="1">          
            
            <CollectionView x:Name="lvElement" Grid.Column="0" Grid.ColumnSpan="3" SelectionMode="None" EmptyView="" Margin="3,0" >
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" NumberOfTapsRequired="1" />
                            </Grid.GestureRecognizers>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Name}"  HorizontalTextAlignment="Start" FontSize="14" FontAttributes="Bold" Padding="2,5" />

                            <Label Text="{Binding Description}" Grid.Column="1" HorizontalTextAlignment="End" TextColor="Gray" FontSize="14" Margin="10,5" LineBreakMode="TailTruncation" MaxLines="1" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>              
            </CollectionView>          
        </Grid>    

    </Grid>   
</ContentPage>